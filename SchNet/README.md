# SchNet (QM9)

This folder contains the QM9 + SchNet ACE experiments.

To run the constrained, non-equivariant setup (example for target `11` (Cv)), run:

`python3 main.py --target_idx 11 --use_constrained 1 --disable_equi 1`
